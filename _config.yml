markdown: kramdown
baseurl:
exclude: ['README.md', 'LICENSE', '_old']
future: true

collections:
  projects:
    output: true
  events:
    output: true
  assets:
    output: true
  GGJProjects:
    output: true

defaults:
  - 
    scope:
      path: ''
      # I have not a single clue what path does send help
      # ok so it turns out the entire config.yml file uses jekyll
      type: projects
    values:
      layout: project_page
      extra_css: project_page.css
  - 
    scope:
      path: ''
      type: events
    values:
      layout: event_page
      extra_css: event_page.css
  - 
    scope:
      path: ''
      type: assets
    values:
      layout: assets_page
      extra_css: asset_page.css
  -
    scope:
      path: ''
      type: GGJProjects
    values:
      layout: 
      extra_css: